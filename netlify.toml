[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # Your app needs this for images:
  VITE_API_BASE = "https://movie-ticket-booking-backend-o1m2.onrender.com/api"
