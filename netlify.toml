[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--include=dev"   # add --loglevel verbose temporarily if debugging

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
