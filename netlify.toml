[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # match your app’s code; use ONE variable only:
  VITE_API_BASE = "https://movie-ticket-booking-backend-o1m2.onrender.com/api"
